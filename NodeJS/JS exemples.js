
/*let browser = prompt(" What browser you would like to use?",'').toLowerCase();
if (browser=="edge") {
    alert ( "You've got the Edge!" );
 } else if(browser == 'chrome','yandex') {
    alert( 'Okay we support these browsers too' );
 } else {
 alert( 'We hope that this page looks ok!' );
 }
    

 const number =+prompt("Enter count between o and 3", '');
switch (number){
   case  0:
   alert("You enter 0");
   break;

   case 1:
   alert("You enter 1");
   break;
   case 2:
   case 3:
   alert("Tou enter 2, meybe 3");
   break;

}

function pow(x, n) {
  let result = x;

  for (let i = 1; i < n; i++) {
    result *= x;
  }

  return result;
}

let x = prompt("x?", '');
let n = prompt("n?", '');

if (n < 1) {
  alert(`Степень ${n} не поддерживается, используйте натуральное число`);
} else {
  alert( pow(x, n) );
}

let result = sum(5, 9);
let message =('sum of 5 and 9 =');
alert(message + result);


function sum (a, b){
  return a + b;
   
}*///------------------------------
/*let result = sum(6, 10);
alert(result);


function sum (a, b){
  return 'sum of ' + a + ' and ' + b + ' = ' + (a*b) ;
//  return `sum of ${a} and ${b} =`+ (a+b) ;

   
}*//*===================================================================
let a = +prompt("enter count", ' ');
let b = +prompt(" degree of construction of the previous number?", '');

function multi(a,b){
  let result = a
  for (let i = 1; i < b; i++){
    result  *= a;
  } 
  return result;
}

if (b < 1) {
  alert (`Значение ${b} не использовать, только целые числа`);
} else {
  alert (multi (a,b) );
}======================================================================*/

 /*function sum (a,b){
   return a+40
   
 }
 let result = sum (10);
 alert (result);
//=============================== */

let numbers = [5,4,8,3,9,6,7];// не работает - исправить!!!!!
function min (numbers) {
for (let i = 0; i < numbers.length; i++){
  console.log(numbers[i])
  }
}




</script>

  